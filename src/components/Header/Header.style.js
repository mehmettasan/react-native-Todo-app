import { StyleSheet } from "react-native";

export default StyleSheet.create({
    header: {
        flexDirection: "row",
        justifyContent: "space-between",
        padding: 20,
        width: "100%"
    },
    header_text: {
        color: "#ffa500",
        fontSize: 30,
        fontWeight: "bold"
    }
})